# Предложения по улучшению приложения

## Статус выполнения (кратко)

В этой ветке выполнены и запушены несколько улучшений: назначение задач, поиск/фильтры (клиентская логика), пагинация, виртуализация длинных списков, подтверждение удаления + undo, оптимистичные обновления, realtime подписки (UI обновляется автоматически), PWA manifest (начальный набор) и миграция‑скрипт для pg_trgm (в репозитории, нужно применить на Supabase).

## TODO (оставшиеся, приоритеты)

Сформирована очередь задач (todos в сессии). Основные задачи, ожидающие реализации:

- notifications — Push / email уведомления для дедлайнов и событий (опционально: интеграция с OneSignal/Firebase/SMTP).
- route-sorting — drag&drop сортировка в магазине + массовая перезапись порядка и экспорт оптимизированного маршрута.
- history-recurrence-ui — управляемые шаблоны часто покупаемого и UI для повторяющихся задач/событий (редактирование recurrence).
- apply-search-indexes — применить миграцию supabase/migrations/0002_add_pg_trgm_indexes.sql на прод (создаёт pg_trgm и GIN индексы).
- rls-audit — провести аудит RLS-политик по всем таблицам и добавить тесты доступа.
- server-validation — добавить ограничения БД (CHECK/NOT NULL/length) и серверную валидацию.
- backups — включить автоматические бэкапы и проверить процедуру восстановления.
- e2e-playwright — написать базовые e2e (login, create family, add item, mark bought) и интегрировать в CI.
- ci-workflow — добавить GitHub Actions: tsc, eslint, build, e2e.
- ui-state-component — вынести общий компонент Loading / Empty / Error.

## Как действовать дальше

1. Если нужно, применить файл миграции для pg_trgm (см. supabase/migrations/0002_add_pg_trgm_indexes.sql). Я могу применить при предоставлении DATABASE_URL/service_role или подготовить PR-инструкцию.
2. Рекомендую сделать CI+e2e и затем работать по очереди: уведомления → route-sorting → history/recurrence.

Файлы/ресурсы:
- supabase/migrations/0002_add_pg_trgm_indexes.sql — миграция для ускорения поиска.
- supabase/README_INDEXES.md — инструкция по применению миграции.

